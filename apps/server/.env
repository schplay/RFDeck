DATABASE_URL="file:./rfdeck.db"
PORT=3000

# Meros Cloud configuration is deliberately NOT here: this file is tracked, and
# the base URL and client id differ per environment. They live in .env.local for
# development and in the systemd unit in production.
# See docs/CLOUD_INTEGRATION_PLAN.md → Identity → "Environment, not constants".
