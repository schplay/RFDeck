{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist"
  },
  "include": ["src/**/*"],
  // preload.ts is compiled separately to CommonJS — Electron loads preload
  // scripts as CJS, but this package is "type": "module".
  "exclude": ["src/preload.ts"]
}
